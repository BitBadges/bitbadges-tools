/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            // ... other domains
            'assets.calendly.com',
        ],
    },
};

export default nextConfig;
